pipeline {
    agent any

    stages {
        stage('pull') {
            steps {
                git branch: 'main', url: 'https://github.com/vaishnaviid/Linux.git'
            }
        }

        stage('welcome') {
            steps {
                echo 'welcome to pipelines'
            }
        }
    }
}

